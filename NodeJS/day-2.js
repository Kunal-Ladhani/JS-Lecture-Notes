const http = require("http");
// CJS common JS syntax -> old JS syntax.
// EJS ES6 JS syntax also there. -> import export etc.
// read on modules and imports

console.log(myName);    // hoisting var myName; - undefined for now
var myName = "Kunal";
console.log(myName);

var age = 26;
var hobby = 'poetry';
// global variables
