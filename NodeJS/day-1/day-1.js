// Node.js is basically a JS runtime environment
// it contains V8 engine inside it to run JS + some other functionalities

// JS is -
// single threaded
// event driven
// concurrent
// multi paradigm - OOP, FP
// TODO: READ ON NODE JS ARCHITECHTURE

// helps us write server side code
// the transpiter had V8 engine embededed in it (written in C++)
// transpiles the JS code to machine code that can run on machine.

// TODO: LEARN COMPILER, TRANSPILER, INTERPRETER, PARSER

// REPL - read eval print loop
// we can write js code here and it will run on node.
// can theoretically write long programs in it
// but its not convienent so we have other way.

// we write the code in .js or .ts files and run it using node file_name.js 


// nodejs has access to all system resources.
// so we can use nodejs for writing backend on machine
// it gives us different functionality than the browser JS

// react on frontend
// node on backend
// express on server
// mongo on database
// full stack can be covered using the JS/TS language only!

console.log("Hello world!");
//alert("starting NodeJS"); -> alert not defined.